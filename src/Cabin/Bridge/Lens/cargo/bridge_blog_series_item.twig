{% if item.series %}
    <li id="series_items_series_{{ item.series|e('html_attr') }}"{#
        #} class="series_item_series"{#
        #} data-itemid="{{ item.itemid|e('html_attr') }}"{#
        #} data-id="series_{{ item.series|e('html_attr') }}"{#
        #} data-type="series"{#
        #} data-rowid="{{ item.series|e('html_attr') }}"{#
    #}>{#
        #}<span>{#
            #}<i class="fa fa-list-alt"></i> {#
            #}{{ item.name }}{#
        #}</span>{#
        #}<a class="delete_item" data-id="series_{{ item.series|e('html_attr') }}">{#
            #}<i class="fa fa-trash"></i>{#
        #}</a>{#
    #}</li>
{% else %}
    <li id="series_items_blogpost_{{ item.post|e('html_attr') }}"{#
        #} class="series_item_blogpost"{#
        #} data-itemid="{{ item.itemid|e('html_attr') }}"{#
        #} data-id="blogpost_{{ item.post|e('html_attr') }}"{#
        #} data-type="blogpost"{#
        #} data-rowid="{{ item.post|e('html_attr') }}"{#
    #}>{#
        #}<span>{#
            #}<i class="fa fa-file-text-o"></i> {#
            #}{{ item.name }}{#
        #}</span>{#
        #}<a class="delete_item" data-id="blogpost_{{ item.post|e('html_attr') }}">{#
            #}<i class="fa fa-trash"></i>{#
        #}</a>{#
    #}</li>
{% endif %}