{# For motifs that fail to override this template. #}
<textarea title="{{ __("Motif Configuration")|e('html_attr') }}" name="motif_config">{{
    motif_config|json_encode(
        constant("JSON_PRETTY_PRINT")
    )
}}</textarea>